<!DOCTYPE html>
<html>

<head>
  <title>PapperHelper</title>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    }

    .container {
      text-align: center;
      padding: 2rem;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.15);
      backdrop-filter: blur(4px);
      width: 90%;
      max-width: 600px;
    }

    h1 {
      font-size: 3.5rem;
      color: #2c3e50;
      margin-bottom: 1rem;
    }

    .slogan {
      font-size: 1.5rem;
      color: #34495e;
      margin-bottom: 2rem;
    }

    .login-message {
      font-size: 1.2rem;
      color: #7f8c8d;
      margin-bottom: 1.5rem;
    }

    .login-btn {
    display: inline-block;
    padding: 1rem 3rem;
    margin-top: 2rem;
    background: #3498db;
    color: white;
    text-decoration: none;
    border-radius: 50px;
    font-size: 1.2rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;  /* 只過渡 transform 和 box-shadow */
}

.login-btn:hover {
    background: #2980b9;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
}
  </style>
</head>

<body>
  <div class="container">
    <h1>PapperHelper</h1>
    <p class="slogan">Research Simplified, Knowledge Amplified</p>
    <p class="login-message">Please log in to continue</p>
    <a href="/login" class="login-btn">Login</a>
  </div>

  <script>
    gsap.from("h1", {
      opacity: 0,
      y: -20,
      duration: 1,
      ease: "power3.out"
    });

    gsap.from(".slogan", {
      opacity: 0,
      y: -20,
      duration: 1,
      delay: 0.3,
      ease: "power3.out"
    });

    gsap.from(".login-message", {
      opacity: 0,
      y: -20,
      duration: 1,
      delay: 0.6,
      ease: "power3.out"
    });

    gsap.from(".login-btn", {
      opacity: 0,
      y: -20,
      duration: 1,
      delay: 0.9,
      ease: "power3.out"
    });
  </script>
</body>

</html>
